"
I am a P2PSocket that wraps a P2PDevice into a Socket so it can be used by a P2PServer.

I provide the same functionality as a Socket but I need to be created differently:

P2PSocket.
"
Class {
	#name : #P2PSocket,
	#superclass : #Object,
	#category : #'IOTConnectivity-P2P-Server-Compat'
}
